---
import BaseLayout from '../layouts/BaseLayout.astro';
import { works } from '../data/works';

const withBase = (path: string) => {
  const base = import.meta.env.BASE_URL.endsWith('/')
    ? import.meta.env.BASE_URL
    : `${import.meta.env.BASE_URL}/`;
  return `${base}${path.replace(/^\/+/, '')}`;
};

const statusLabel = {
  live: 'Live',
  reserved: 'Reserved',
  draft: 'Draft',
  maintained: 'Maintained'
};
---
<BaseLayout title="Works" description="Project and experiment catalogue for Misaka Sarina digital traces.">
  <section class="section card works-hero">
    <div>
      <p class="eyebrow" data-i18n="works.archive">Works Archive</p>
      <h1 class="title" data-i18n="works.title">Projects, systems, and experiments.</h1>
      <p class="copy" data-i18n="works.copy">A lightweight index for public-facing web tools, archive systems, music infrastructure, and technical experiments around the Misaka Sarina / Siacone Art space.</p>
    </div>
    <div class="stats" aria-label="Works archive stats">
      <div><span>Total</span><strong>{works.length}</strong></div>
      <div><span>Live</span><strong>{works.filter((work) => work.status === 'live').length}</strong></div>
      <div><span>Maintained</span><strong>{works.filter((work) => work.status === 'maintained').length}</strong></div>
    </div>
  </section>

  <section class="section works-list" aria-label="Works list">
    {works.map((work) => (
      <article class="card work-card">
        <div class="work-main">
          <p class="eyebrow">{work.category.replace('-', ' ')}</p>
          <h2>{work.title}</h2>
          <p>{work.summary}</p>
          <div class="tags">
            {work.tags.map((tag) => <span>{tag}</span>)}
          </div>
        </div>
        <div class="work-side">
          <strong>{statusLabel[work.status]}</strong>
          <div class="button-column">
            <a class="button primary" href={withBase(`/works/${work.slug}/`)} data-i18n="works.details">Details</a>
            {work.href && <a class="button" href={work.href.startsWith('/') ? withBase(work.href) : work.href} data-i18n="works.open">Open</a>}
          </div>
        </div>
      </article>
    ))}
  </section>

  <section class="section card next-card">
    <p class="eyebrow" data-i18n="works.next">Next step</p>
    <h2 data-i18n="works.nextTitle">Works now have shareable detail pages.</h2>
    <p class="copy" data-i18n="works.nextCopy">Each work entry now has a generated detail route. The next expansion can add screenshots, repository links, changelogs, deployment notes, and maintenance history.</p>
    <div class="button-row">
      <a class="button primary" href={withBase('/blog')} data-i18n="works.readNotes">Read notes</a>
      <a class="button" href={withBase('/contact')} data-i18n="home.contact">Contact</a>
    </div>
  </section>
</BaseLayout>
<style>
  .works-hero {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 0.42fr);
    gap: 28px;
    align-items: end;
    padding: clamp(28px, 6vw, 56px);
  }

  .stats {
    display: grid;
    gap: 12px;
  }

  .stats div,
  .work-card,
  .next-card {
    border: 1px solid rgba(236, 238, 255, 0.12);
  }

  .stats div {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    border-radius: 18px;
    background: rgba(7, 8, 18, 0.54);
    padding: 14px 16px;
  }

  .stats span,
  .tags span {
    color: var(--soft);
    font-size: 0.72rem;
    font-weight: 850;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .stats strong,
  .work-side strong {
    color: var(--accent);
  }

  .stats strong {
    font-size: 1.45rem;
    line-height: 1;
  }

  .works-list {
    display: grid;
    gap: 18px;
  }

  .work-card {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 180px;
    gap: 20px;
    padding: 24px;
  }

  .work-main h2,
  .next-card h2 {
    margin: 0 0 12px;
    font-size: clamp(1.7rem, 4vw, 2.7rem);
    line-height: 0.96;
    letter-spacing: -0.06em;
  }

  .work-main p {
    margin: 0;
    color: var(--muted);
    line-height: 1.72;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 16px;
  }

  .tags span {
    border: 1px solid rgba(236, 238, 255, 0.12);
    border-radius: 999px;
    background: rgba(7, 8, 18, 0.48);
    padding: 8px 10px;
  }

  .work-side {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    flex-direction: column;
    gap: 14px;
  }

  .button-column {
    display: grid;
    gap: 10px;
    width: 100%;
  }

  .next-card {
    padding: clamp(28px, 6vw, 44px);
  }

  @media (max-width: 820px) {
    .works-hero,
    .work-card {
      grid-template-columns: 1fr;
    }

    .work-side {
      align-items: flex-start;
    }

    .button-column {
      width: auto;
    }
  }
</style>
