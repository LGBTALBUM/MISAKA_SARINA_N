---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { webPageJsonLd } from '../../utils/seo';
const description = 'Worker-backed Bluesky RSS feed rendered as full-width cards.';
---
<BaseLayout
  title="Bluesky RSS"
  description={description}
  tags={['Bluesky', 'RSS', 'OpenRSS', 'Cloudflare Worker']}
  jsonLd={webPageJsonLd({ title: 'Bluesky RSS', description, path: '/blog/bluesky/', type: 'CollectionPage' })}
>
  <link rel="stylesheet" href